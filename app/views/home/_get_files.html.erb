<div id="show" class= "user_functionality">
 Your files are as shown below
  <ul>
  <% @lists.each do |l|%>
    <li>
     <%= link_to l, :action => 'download', :data => @user + ":" + l%>
    </li>
  <%end%>
  </ul>
</div>
